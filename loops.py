# Use loop to calculate the sum of the numbers below
numbers = [10, 5, 20, 8, 15]
# sum = 0

# for number in numbers:
#     sum += number
#     print(sum)        #also displays the calculation

sum = 0
for number in numbers:
    sum = sum + number
print(sum)